# Examples

Demo (no API key):

- `python_demo.py`
- `curl_demo.sh`

Production (requires `X-API-Key`):

- `python_production.py`
- `python_batch.py`
- `curl_production.sh`
- `curl_batch.sh`

Environment variables:

- `FILTERDATA_API_KEY` (or `API_KEY` for Python scripts)

